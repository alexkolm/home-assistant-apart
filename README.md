# 🏠 Home Assistant — конфигурация умной квартиры

> Автоматизации, шаблоны и архитектурные решения для реальной системы Home Assistant, работающей в квартире на HAOS.

Это **не бэкап**, а витрина логики: рабочие automations, скрипты диагностики и подход к организации конфигурации. Полные снапшоты HAOS, `secrets.yaml` и runtime-данные хранятся отдельно и в этот репозиторий не попадают.

---

## 🎯 Зачем этот репозиторий

- Версионировать **логику автоматизаций**, а не состояние системы
- Фиксировать архитектурные решения (почему сделано так, а не иначе)
- Использовать Git как инструмент проектирования и код-ревью, а не как способ восстановления HAOS после сбоя
- Дать пример рабочей, а не "туториальной" конфигурации: с реальными edge-кейсами, защитой от ложных срабатываний и уведомлениями

## ⚙️ Что реально автоматизировано

Конфигурация покрывает несколько практических сценариев умной квартиры:

| Категория | Что делает |
|---|---|
| **Безопасность и протечки** | Сенсоры протечки в ванной, кухне и под батареями мгновенно перекрывают воду/электричество через `switch` и шлют голосовое оповещение на Яндекс.Станцию + Telegram |
| **Диагностика Zigbee2MQTT** | Python-скрипт `check_zigbee.py` раз в час проверяет свежесть `state.json` и `last_seen` каждого устройства; при проблеме — не более одного уведомления в сутки, без спама |
| **Присутствие и "режим отъезда"** | Приход/уход хозяина (`device_tracker`) включает/выключает оповещения об открытии двери; двойное нажатие на выключателе переводит квартиру в режим "уехали надолго" — снижает температуру, выключает бойлер и краны |
| **Освещение по выключателям и датчикам движения** | Zigbee-выключатели (Tuya/Aqara-стиль, через MQTT-топики) управляют светом в спальне, коридоре, у раковины и в туалете; коридор дополнительно реагирует на движение с учётом освещённости |
| **Климат и коммуникации** | Управление тёплым полом по расписанию с ограничением по времени суток; пересылка сообщений Meshtastic-радиосети в Telegram |
| **Бытовые напоминания** | Контроль влажности почвы у растений, автоотключение люстр, если горят дольше 5 часов, отметка заката для проверки, что сервер/HA живы |

Все уведомления идут через `notify.me` (Telegram) и, для критичных событий, дублируются голосом через `media_player.play_media` на Яндекс.Станцию.

## 📁 Структура репозитория

В репозиторий намеренно попадают только root-конфигурации `/config`:

```
.
├── configuration.yaml   # точка входа, интеграции, shell_command
├── automations.yaml     # вся логика автоматизаций (см. таблицу выше)
├── scenes.yaml          # сценарии освещения/климата
├── scripts.yaml         # HA-скрипты (пока не используется)
├── check_zigbee.py       # диагностика state.json Zigbee2MQTT, вызывается по shell_command
├── known_devices.yaml   # device_tracker для присутствия
└── ui-lovelace.yaml     # пример пользовательского интерфейса
```

## ❌ Что намеренно исключено

- `secrets.yaml`, пароли, токены, `chat_id`
- IP- и MAC-адреса устройств
- Видеопотоки и файлы камер
- Базы данных (`.db`), логи, кэш, runtime-файлы HAOS

## 🔐 Секреты и приватность

Все чувствительные значения вынесены в `secrets.yaml` и в репозиторий не попадают (см. `!secret` в `automations.yaml`). Репозиторий безопасен для публичного просмотра — но конкретные `entity_id` показывают реальную топологию квартиры, поэтому это в первую очередь витрина подхода, а не шаблон для копирования один в один.

## 🧠 Принципы конфигурации

- **Git → идеи и логика, Backup → восстановление системы** — жёсткое разделение
- Минимальная привязка к конкретному оборудованию — где возможно, используются `switch`/`light` абстракции, а не вендорские сервисы
- Явная обработка исключительных ситуаций: анти-дребезг (`for: seconds`), защита от повторных уведомлений (`input_datetime` как троттлинг), fallback-ветки в `choose`
- Голосовые оповещения зарезервированы только для событий, требующих немедленной реакции (протечка); остальное — тихий Telegram

## 🔄 Как использовать этот репозиторий

Репозиторий не предназначен для прямого копирования — `entity_id`, топики MQTT и `device_tracker` завязаны на конкретное железо.

1. Использовать как референс архитектуры и паттернов
2. Адаптировать `entity_id`/топики под своё оборудование
3. Завести собственный `secrets.yaml`
4. Проверить и протестировать через UI Home Assistant перед продакшеном

## 🚧 Статус

Отражает реальную работающую систему в текущий момент; структура и набор автоматизаций меняются по мере развития умного дома.

## 📌 TODO

- [ ] Описать общую архитектуру системы (диаграмма интеграций)
- [ ] Задокументировать ключевые automations по отдельности
- [ ] Вынести повторяющиеся шаблоны (протечки, освещение по кнопке) в blueprints
- [ ] Добавить упрощённый пример конфигурации для тех, кто хочет стартовать с нуля
